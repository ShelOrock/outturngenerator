import React from 'react';

import { GenericComponentProps } from '../../types';
import { PageContainers } from '../Containers';

interface ComponentProps extends GenericComponentProps {

};

const Outturns: React.FC<ComponentProps> = ({
  
}) => (
  <PageContainers.Main>
    
  </PageContainers.Main>
);
